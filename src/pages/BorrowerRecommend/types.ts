export interface BookCategoryItem {
  uri: string;
  title: string;
  key: number;
}

export interface BookRecommendItem {
  id: number;
  title: string;
  imgUrl: string;
  desc: string;
}

export interface SwiperItem {
  id: number;
  uri: string;
}
